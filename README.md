# Prompt-Injection

This repository contains experimental work on detecting and analyzing prompt injection in transformer-based large language models.

The project explores whether prompt injection can be identified through:

Logit shift analysis (clean vs. injected prompts)

Hidden-state cosine distance across intermediate layers

Internal representation changes beyond the final output layer

The goal is to investigate whether injection effects are observable using different methods.
